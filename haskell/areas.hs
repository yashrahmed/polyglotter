import qualified Areas.Circle as Circle
import qualified Areas.Square as Square


-- module Areas(
--     circleArea,
--     squareArea
-- ) 
-- where

-- squareArea :: (Num a) => a -> a
-- squareArea x = x * x

-- circleArea :: (Floating a) => a -> a
-- circleArea x = pi * ((x * x))
